import { Injectable } from '@nestjs/common';

Injectable();
export class EnvelopeService {
  maxHappyUsers(userCount: number, envelopeCount: number): number {
    // TODO: implement me
    return -1;
  }
}
